<div>
<%= link_to 'New Article',
            new_article_path,
            class: 'new-article-button btn btn-block btn-primary' %>
</div>

<br>

<%= will_paginate @articles %>

<%= render @articles %>

<%= will_paginate @articles %>
